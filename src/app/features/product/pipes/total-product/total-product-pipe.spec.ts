import { TotalProductPipe } from './total-product-pipe';

describe('TotalProductPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalProductPipe();
    expect(pipe).toBeTruthy();
  });
});
